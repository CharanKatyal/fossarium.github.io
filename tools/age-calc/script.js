const dob=document.getElementById("dob"),result=document.getElementById("result");dob.value="2000-01-01";function calc(){const b=new Date(dob.value),now=new Date();if(isNaN(b)){result.textContent="Enter a valid date";return;}let y=now.getFullYear()-b.getFullYear(),m=now.getMonth()-b.getMonth(),d=now.getDate()-b.getDate();if(d<0){m--;d+=new Date(now.getFullYear(),now.getMonth(),0).getDate();}if(m<0){y--;m+=12;}const days=Math.floor((now-b)/(86400000));const weeks=Math.floor(days/7);result.innerHTML=`<div style="font-size:2rem;font-weight:900;color:var(--accent-color);margin-bottom:10px">${y} years, ${m} months, ${d} days</div><div style="color:var(--text-muted)">Total: ${days.toLocaleString()} days (${weeks.toLocaleString()} weeks)</div>`;}dob.addEventListener("input",calc);calc();
