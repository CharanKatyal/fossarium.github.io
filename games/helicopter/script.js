const canvas=document.getElementById("canvas"),ctx=canvas.getContext("2d"),W=400,H=300;let heli={y:H/2,vy:0},pipes=[],score=0,best=0,hold=false,running=false,frame=0;function init(){heli={y:H/2,vy:0};pipes=[];score=0;frame=0;running=true;for(let i=0;i<5;i++)pipes.push({x:W+i*120,gap:80+Math.random()*40,gapY:60+Math.random()*(H-120)});}canvas.addEventListener("mousedown",()=>{hold=true;if(!running)init();});canvas.addEventListener("mouseup",()=>hold=false);document.addEventListener("keydown",e=>{if(e.code==="Space"){hold=true;if(!running)init();}});document.addEventListener("keyup",e=>{if(e.code==="Space")hold=false;});function tick(){if(!running){ctx.clearRect(0,0,W,H);ctx.fillStyle="#fff";ctx.font="bold 20px Inter";ctx.textAlign="center";ctx.fillText("Click or hold Space to start",W/2,H/2);requestAnimationFrame(tick);return;}ctx.clearRect(0,0,W,H);heli.vy+=hold?-0.5:0.4;heli.y+=heli.vy;if(heli.y<0||heli.y>H){running=false;best=Math.max(best,score);document.getElementById("best").textContent=best;}ctx.fillStyle="#58a6ff";ctx.fillRect(60,heli.y-8,25,16);ctx.fillStyle="rgba(88,166,255,.4)";ctx.fillRect(50,heli.y-5,12,10);pipes.forEach(p=>{p.x-=2;if(p.x<-30){p.x=W+50;p.gap=80+Math.random()*40;p.gapY=60+Math.random()*(H-120);score++;document.getElementById("score").textContent=score;}ctx.fillStyle="#2ed573";ctx.fillRect(p.x,0,25,p.gapY);ctx.fillRect(p.x,p.gapY+p.gap,25,H-p.gapY-p.gap);if(60+25>p.x&&60<p.x+25&&(heli.y-8<p.gapY||heli.y+8>p.gapY+p.gap)){running=false;best=Math.max(best,score);document.getElementById("best").textContent=best;}});frame++;requestAnimationFrame(tick);}tick();
