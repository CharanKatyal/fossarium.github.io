const SIZE=5,pipes=["┃","━","┗","┛","┓","┏","┣","┫","┳","┻","╋"];let grid=[],level=1;function newLevel(){grid=[];for(let i=0;i<SIZE;i++){grid[i]=[];for(let j=0;j<SIZE;j++){grid[i][j]={type:Math.floor(Math.random()*pipes.length),rot:Math.floor(Math.random()*4)};}}render();}function render(){const gEl=document.getElementById("grid");gEl.style.gridTemplateColumns=`repeat(${SIZE},1fr)`;gEl.innerHTML="";for(let i=0;i<SIZE;i++)for(let j=0;j<SIZE;j++){const d=document.createElement("div");d.className="pipe";d.textContent=pipes[grid[i][j].type];d.style.transform=`rotate(${grid[i][j].rot*90}deg)`;d.addEventListener("click",()=>{grid[i][j].rot=(grid[i][j].rot+1)%4;render();});gEl.appendChild(d);}document.getElementById("level").textContent=level;}newLevel();
