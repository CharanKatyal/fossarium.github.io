let count=0,timeLeft=10,best=0,timer=null,started=false;const btn=document.getElementById("tap-btn");btn.addEventListener("click",()=>{if(!started){started=true;timer=setInterval(()=>{timeLeft--;document.getElementById("time").textContent=timeLeft;if(timeLeft<=0){clearInterval(timer);btn.classList.add("disabled");best=Math.max(best,count);document.getElementById("best").textContent=best;document.getElementById("result").textContent="🎉 "+count+" taps! ("+((count/10).toFixed(1))+" taps/sec)";setTimeout(()=>{count=0;timeLeft=10;started=false;btn.classList.remove("disabled");document.getElementById("count").textContent=0;document.getElementById("time").textContent=10;document.getElementById("result").textContent="";},2000);}},1000);}count++;document.getElementById("count").textContent=count;});
