const canvas=document.getElementById("canvas"),ctx=canvas.getContext("2d"),S=400,CELL=8,N=S/CELL;let grid=Array.from({length:N},()=>Array(N).fill(0)),gen=0,playing=false,interval=null;function draw(){ctx.clearRect(0,0,S,S);let alive=0;for(let r=0;r<N;r++)for(let c=0;c<N;c++){if(grid[r][c]){alive++;ctx.fillStyle="rgba(88,166,255,"+(0.5+Math.random()*0.5)+")";ctx.fillRect(c*CELL,r*CELL,CELL-1,CELL-1);}}document.getElementById("gen").textContent=gen;document.getElementById("alive").textContent=alive;}function step(){const next=Array.from({length:N},()=>Array(N).fill(0));for(let r=0;r<N;r++)for(let c=0;c<N;c++){let n=0;for(let dr=-1;dr<=1;dr++)for(let dc=-1;dc<=1;dc++){if(!dr&&!dc)continue;const nr=(r+dr+N)%N,nc=(c+dc+N)%N;n+=grid[nr][nc];}if(grid[r][c])next[r][c]=n===2||n===3?1:0;else next[r][c]=n===3?1:0;}grid=next;gen++;draw();}function togglePlay(){playing=!playing;document.getElementById("play-btn").textContent=playing?"⏸ Pause":"▶ Play";document.getElementById("status").textContent=playing?"Running":"Paused";if(playing)interval=setInterval(step,100);else clearInterval(interval);}function randomize(){grid=Array.from({length:N},()=>Array.from({length:N},()=>Math.random()>.7?1:0));gen=0;draw();}function clearGrid(){grid=Array.from({length:N},()=>Array(N).fill(0));gen=0;draw();}canvas.addEventListener("click",e=>{const rect=canvas.getBoundingClientRect();const c=Math.floor((e.clientX-rect.left)/CELL),r=Math.floor((e.clientY-rect.top)/CELL);if(r>=0&&r<N&&c>=0&&c<N){grid[r][c]=1-grid[r][c];draw();}});randomize();
