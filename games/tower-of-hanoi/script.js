const colors=["#ff4757","#ffa502","#2ed573","#1e90ff","#a55eea","#ff6b81","#eccc68"];let pegs=[[],[],[]],moves=0,selected=null,numDiscs=5;function newGame(){numDiscs=parseInt(document.getElementById("disc-count").value);pegs=[[],[],[]];moves=0;for(let i=numDiscs;i>=1;i--)pegs[0].push(i);document.getElementById("discs").textContent=numDiscs;render();}function render(){const el=document.getElementById("towers");el.innerHTML="";pegs.forEach((peg,pi)=>{const tower=document.createElement("div");tower.className="tower";tower.addEventListener("click",()=>clickTower(pi));peg.forEach(disc=>{const d=document.createElement("div");d.className="disc"+(selected!==null&&pegs[selected][pegs[selected].length-1]===disc?" selected":"");d.style.width=(30+disc*20)+"px";d.style.background=colors[(disc-1)%colors.length];tower.appendChild(d);});el.appendChild(tower);});document.getElementById("moves").textContent=moves;}function clickTower(pi){if(selected===null){if(pegs[pi].length>0)selected=pi;}else{if(pi===selected){selected=null;}else{const from=pegs[selected],to=pegs[pi];if(to.length===0||to[to.length-1]>from[from.length-1]){to.push(from.pop());moves++;if(pegs[2].length===numDiscs){setTimeout(()=>alert("🎉 Solved in "+moves+" moves! (Optimal: "+(Math.pow(2,numDiscs)-1)+")"),100);}selected=null;}else{selected=pi;}}render();}newGame();
