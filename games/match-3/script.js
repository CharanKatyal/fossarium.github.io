const SIZE=8,GEMS=["🔴","🟢","🔵","🟡","🟣","🟠"];let grid=[],selected=null,score=0,moves=30;function init(){grid=[];for(let r=0;r<SIZE;r++){grid[r]=[];for(let c=0;c<SIZE;c++)grid[r][c]=Math.floor(Math.random()*GEMS.length);}removeMatches();render();}function render(){const el=document.getElementById("grid");el.innerHTML="";for(let r=0;r<SIZE;r++)for(let c=0;c<SIZE;c++){const d=document.createElement("div");d.className="gem"+(selected&&selected.r===r&&selected.c===c?" selected":"");d.textContent=GEMS[grid[r][c]];d.style.background=`rgba(${[255,46,88,255,168,255][grid[r][c]]},${[71,213,166,165,85,107][grid[r][c]]},${[87,115,255,0,234,72][grid[r][c]]},.15)`;d.addEventListener("click",()=>pick(r,c));el.appendChild(d);}document.getElementById("score").textContent=score;document.getElementById("moves").textContent=moves;}function pick(r,c){if(!selected){selected={r,c};render();return;}const dr=Math.abs(selected.r-r),dc=Math.abs(selected.c-c);if(dr+dc===1){[grid[selected.r][selected.c],grid[r][c]]=[grid[r][c],grid[selected.r][selected.c]];const removed=removeMatches();if(removed===0)[grid[selected.r][selected.c],grid[r][c]]=[grid[r][c],grid[selected.r][selected.c]];else{moves--;score+=removed*10;}}selected=null;render();}function removeMatches(){let removed=0;const toRemove=new Set();for(let r=0;r<SIZE;r++)for(let c=0;c<SIZE-2;c++){if(grid[r][c]===grid[r][c+1]&&grid[r][c]===grid[r][c+2]){toRemove.add(r+","+c);toRemove.add(r+","+(c+1));toRemove.add(r+","+(c+2));}}for(let c=0;c<SIZE;c++)for(let r=0;r<SIZE-2;r++){if(grid[r][c]===grid[r+1][c]&&grid[r][c]===grid[r+2][c]){toRemove.add(r+","+c);toRemove.add((r+1)+","+c);toRemove.add((r+2)+","+c);}}toRemove.forEach(k=>{const[r,c]=k.split(",").map(Number);grid[r][c]=-1;removed++;});if(removed>0){for(let c=0;c<SIZE;c++){let empty=0;for(let r=SIZE-1;r>=0;r--){if(grid[r][c]===-1)empty++;else if(empty>0){grid[r+empty][c]=grid[r][c];grid[r][c]=-1;}}for(let r=0;r<empty;r++)grid[r][c]=Math.floor(Math.random()*GEMS.length);}setTimeout(()=>{const more=removeMatches();if(more>0){score+=more*10;render();}},200);}return removed;}init();
