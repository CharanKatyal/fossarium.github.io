const canvas=document.getElementById("canvas"),ctx=canvas.getContext("2d"),W=350,C=W/2,R=12;const positions=[];for(let ring=0;ring<5;ring++){const count=ring===0?1:ring*6;const dist=ring*30;for(let i=0;i<count;i++){const angle=(i/count)*Math.PI*2-Math.PI/2;positions.push({x:C+Math.cos(angle)*dist,y:C+Math.sin(angle)*dist,piece:ring<2?1:ring>3?2:0});}}let selected=null,moves=0;function draw(){ctx.clearRect(0,0,W,W);positions.forEach((p,i)=>{ctx.beginPath();ctx.arc(p.x,p.y,R,0,Math.PI*2);ctx.fillStyle=p.piece===1?"#ff4757":p.piece===2?"#2ed573":"rgba(255,255,255,.1)";ctx.fill();if(i===selected){ctx.strokeStyle="#ffa502";ctx.lineWidth=3;ctx.stroke();}else{ctx.strokeStyle="rgba(255,255,255,.15)";ctx.lineWidth=1;ctx.stroke();}});document.getElementById("moves").textContent=moves;}canvas.addEventListener("click",e=>{const rect=canvas.getBoundingClientRect();const mx=e.clientX-rect.left,my=e.clientY-rect.top;let clicked=-1;positions.forEach((p,i)=>{if(Math.hypot(p.x-mx,p.y-my)<R)clicked=i;});if(clicked<0)return;if(selected===null){if(positions[clicked].piece===1)selected=clicked;}else{if(positions[clicked].piece===0){positions[clicked].piece=positions[selected].piece;positions[selected].piece=0;moves++;selected=null;}else if(positions[clicked].piece===1){selected=clicked;}else{selected=null;}}draw();});function newGame(){positions.forEach((p,i)=>{p.piece=i<7?1:i>=positions.length-6?2:0;});selected=null;moves=0;draw();}newGame();
