const COLS=7,ROWS=6,COLORS=["#ff4757","#ffa502","#2ed573","#1e90ff","#a55eea","#ff6b81"];let grid=[],score=0,moves=0,sel=null;function newGame(){grid=[];score=0;moves=0;sel=null;for(let r=0;r<ROWS;r++){grid[r]=[];for(let c=0;c<COLS;c++)grid[r][c]=Math.floor(Math.random()*COLORS.length);}render();}function render(){const el=document.getElementById("grid");el.innerHTML="";for(let r=0;r<ROWS;r++){const row=document.createElement("div");row.className="hex-row";if(r%2)row.style.marginLeft="22px";for(let c=0;c<COLS;c++){const h=document.createElement("div");h.className="hex"+(sel&&sel.r===r&&sel.c===c?" selected":"");h.style.background=COLORS[grid[r][c]];h.addEventListener("click",()=>pick(r,c));row.appendChild(h);}el.appendChild(row);}document.getElementById("score").textContent=score;document.getElementById("moves").textContent=moves;}function pick(r,c){if(!sel){sel={r,c};render();return;}const dr=Math.abs(sel.r-r),dc=Math.abs(sel.c-c);if(dr<=1&&dc<=1&&!(dr===0&&dc===0)){[grid[sel.r][sel.c],grid[r][c]]=[grid[r][c],grid[sel.r][sel.c]];moves++;let matched=removeMatches();if(!matched)[grid[sel.r][sel.c],grid[r][c]]=[grid[r][c],grid[sel.r][sel.c]],moves--;}sel=null;render();}function removeMatches(){let found=false;for(let r=0;r<ROWS;r++)for(let c=0;c<COLS-2;c++){if(grid[r][c]===grid[r][c+1]&&grid[r][c]===grid[r][c+2]&&grid[r][c]>=0){grid[r][c]=grid[r][c+1]=grid[r][c+2]=-1;score+=30;found=true;}}for(let r=0;r<ROWS;r++)for(let c=0;c<COLS;c++)if(grid[r][c]<0)grid[r][c]=Math.floor(Math.random()*COLORS.length);return found;}newGame();
