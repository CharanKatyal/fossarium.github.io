const canvas=document.getElementById("canvas"),ctx=canvas.getContext("2d"),W=400,H=500;let marbles=[],pegs=[],count=0;for(let r=0;r<10;r++)for(let c=0;c<8;c++){pegs.push({x:c*50+25+(r%2?25:0),y:r*45+60});}canvas.addEventListener("click",e=>{const r=canvas.getBoundingClientRect();marbles.push({x:e.clientX-r.left,y:10,vx:(Math.random()-0.5)*2,vy:0,r:6,color:`hsl(${Math.random()*360},70%,60%)`});count++;document.getElementById("count").textContent=count;});function tick(){ctx.clearRect(0,0,W,H);pegs.forEach(p=>{ctx.beginPath();ctx.arc(p.x,p.y,5,0,Math.PI*2);ctx.fillStyle="rgba(255,255,255,.15)";ctx.fill();});marbles=marbles.filter(m=>{m.vy+=0.2;m.x+=m.vx;m.y+=m.vy;pegs.forEach(p=>{const dx=m.x-p.x,dy=m.y-p.y,d=Math.hypot(dx,dy);if(d<m.r+5){const nx=dx/d,ny=dy/d;m.x=p.x+nx*(m.r+5);m.y=p.y+ny*(m.r+5);const dot=m.vx*nx+m.vy*ny;m.vx-=1.5*dot*nx;m.vy-=1.5*dot*ny;m.vx*=0.9;m.vy*=0.9;}});if(m.x<m.r){m.x=m.r;m.vx*=-0.7;}if(m.x>W-m.r){m.x=W-m.r;m.vx*=-0.7;}ctx.beginPath();ctx.arc(m.x,m.y,m.r,0,Math.PI*2);ctx.fillStyle=m.color;ctx.fill();ctx.beginPath();ctx.arc(m.x-2,m.y-2,m.r*0.3,0,Math.PI*2);ctx.fillStyle="rgba(255,255,255,.3)";ctx.fill();return m.y<H+20;});requestAnimationFrame(tick);}tick();
