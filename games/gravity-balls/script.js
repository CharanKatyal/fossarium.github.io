const canvas=document.getElementById("canvas"),ctx=canvas.getContext("2d"),W=400,H=500;const colors=["#ff4757","#ffa502","#2ed573","#1e90ff","#a55eea","#ff6b81","#eccc68"];let balls=[];canvas.addEventListener("click",e=>{const r=canvas.getBoundingClientRect();for(let i=0;i<3;i++)balls.push({x:e.clientX-r.left,y:e.clientY-r.top,vx:(Math.random()-0.5)*5,vy:(Math.random()-0.5)*3,r:5+Math.random()*15,color:colors[Math.floor(Math.random()*colors.length)],damping:0.8+Math.random()*0.15});});function tick(){ctx.fillStyle="rgba(22,27,34,.2)";ctx.fillRect(0,0,W,H);balls.forEach(b=>{b.vy+=0.3;b.x+=b.vx;b.y+=b.vy;if(b.y+b.r>H){b.y=H-b.r;b.vy*=-b.damping;}if(b.x-b.r<0){b.x=b.r;b.vx*=-b.damping;}if(b.x+b.r>W){b.x=W-b.r;b.vx*=-b.damping;}ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,Math.PI*2);ctx.fillStyle=b.color;ctx.fill();ctx.beginPath();ctx.arc(b.x-b.r*0.3,b.y-b.r*0.3,b.r*0.3,0,Math.PI*2);ctx.fillStyle="rgba(255,255,255,.2)";ctx.fill();});if(balls.length>200)balls.splice(0,10);requestAnimationFrame(tick);}for(let i=0;i<5;i++)balls.push({x:W/2+Math.random()*50,y:50,vx:(Math.random()-0.5)*4,vy:0,r:8+Math.random()*12,color:colors[Math.floor(Math.random()*colors.length)],damping:0.85});tick();
