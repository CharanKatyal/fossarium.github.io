const canvas=document.getElementById("canvas"),ctx=canvas.getContext("2d"),W=400,H=400;let drawing=false,lastX=0,lastY=0,startX=0,startY=0;ctx.fillStyle="#1a1a2e";ctx.fillRect(0,0,W,H);ctx.lineCap="round";ctx.lineJoin="round";function getPos(e){const r=canvas.getBoundingClientRect();return{x:e.clientX-r.left,y:e.clientY-r.top};}canvas.addEventListener("mousedown",e=>{drawing=true;const p=getPos(e);lastX=startX=p.x;lastY=startY=p.y;});canvas.addEventListener("mousemove",e=>{if(!drawing)return;const p=getPos(e);const tool=document.getElementById("tool").value;const size=document.getElementById("size").value;const color=document.getElementById("color").value;if(tool==="pen"){ctx.strokeStyle=color;ctx.lineWidth=size;ctx.beginPath();ctx.moveTo(lastX,lastY);ctx.lineTo(p.x,p.y);ctx.stroke();}else if(tool==="eraser"){ctx.strokeStyle="#1a1a2e";ctx.lineWidth=size*2;ctx.beginPath();ctx.moveTo(lastX,lastY);ctx.lineTo(p.x,p.y);ctx.stroke();}lastX=p.x;lastY=p.y;});canvas.addEventListener("mouseup",e=>{if(!drawing)return;drawing=false;const p=getPos(e);const tool=document.getElementById("tool").value;const size=document.getElementById("size").value;const color=document.getElementById("color").value;if(tool==="line"){ctx.strokeStyle=color;ctx.lineWidth=size;ctx.beginPath();ctx.moveTo(startX,startY);ctx.lineTo(p.x,p.y);ctx.stroke();}else if(tool==="rect"){ctx.strokeStyle=color;ctx.lineWidth=size;ctx.strokeRect(startX,startY,p.x-startX,p.y-startY);}else if(tool==="circle"){const r=Math.hypot(p.x-startX,p.y-startY);ctx.strokeStyle=color;ctx.lineWidth=size;ctx.beginPath();ctx.arc(startX,startY,r,0,Math.PI*2);ctx.stroke();}});window.clearCanvas=function(){ctx.fillStyle="#1a1a2e";ctx.fillRect(0,0,W,H);};window.saveCanvas=function(){const a=document.createElement("a");a.download="drawing.png";a.href=canvas.toDataURL();a.click();};
