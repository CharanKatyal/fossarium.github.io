const canvas=document.getElementById("canvas"),ctx=canvas.getContext("2d"),W=400;let targets=[],score=0,misses=0,timer=null,timeLeft=30,running=false;function startGame(){targets=[];score=0;misses=0;timeLeft=30;running=true;document.getElementById("score").textContent=0;document.getElementById("misses").textContent=0;document.getElementById("time").textContent=30;clearInterval(timer);timer=setInterval(()=>{timeLeft--;document.getElementById("time").textContent=timeLeft;if(timeLeft<=0){running=false;clearInterval(timer);alert("Score: "+score);}},1000);spawnLoop();}function spawnLoop(){if(!running)return;targets.push({x:30+Math.random()*(W-60),y:30+Math.random()*(W-60),r:15+Math.random()*15,life:60+Math.random()*40});setTimeout(spawnLoop,400+Math.random()*600);}canvas.addEventListener("click",e=>{if(!running)return;const rect=canvas.getBoundingClientRect();const mx=e.clientX-rect.left,my=e.clientY-rect.top;let hit=false;targets=targets.filter(t=>{if(Math.hypot(t.x-mx,t.y-my)<t.r){score++;hit=true;document.getElementById("score").textContent=score;return false;}return true;});if(!hit){misses++;document.getElementById("misses").textContent=misses;}});function tick(){ctx.clearRect(0,0,W,W);targets=targets.filter(t=>{t.life--;const alpha=t.life/100;ctx.beginPath();ctx.arc(t.x,t.y,t.r,0,Math.PI*2);ctx.strokeStyle=`rgba(255,71,87,${alpha})`;ctx.lineWidth=2;ctx.stroke();ctx.beginPath();ctx.arc(t.x,t.y,3,0,Math.PI*2);ctx.fillStyle=`rgba(255,71,87,${alpha})`;ctx.fill();ctx.beginPath();ctx.moveTo(t.x-t.r,t.y);ctx.lineTo(t.x+t.r,t.y);ctx.moveTo(t.x,t.y-t.r);ctx.lineTo(t.x,t.y+t.r);ctx.strokeStyle=`rgba(255,71,87,${alpha*0.5})`;ctx.lineWidth=1;ctx.stroke();return t.life>0;});requestAnimationFrame(tick);}tick();
